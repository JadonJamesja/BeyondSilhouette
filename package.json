{
  "name": "beyond-silhouette-monorepo",
  "private": true,
  "scripts": {
    "build": "npm --prefix server install && cd server && npx prisma generate",
    "start": "cd server && npx prisma migrate deploy && npm start"
  }
}

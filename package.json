{
  "name": "beyond-silhouette-monorepo",
  "private": true,
  "scripts": {
    "build": "npm --prefix server install && cd server && npx prisma generate && npx prisma migrate deploy",
    "start": "npm --prefix server start"
  }
}
